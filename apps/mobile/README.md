# Mobile App (Planned)

This folder is reserved for the React Native app that will be published on Play Store.

Start here:
1. Create app with Expo (`npx create-expo-app@latest apps/mobile`)
2. Connect to backend endpoints in `apps/api`
3. Follow `docs/PLAYSTORE_PUBLISHING.md` for release process
